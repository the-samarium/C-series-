#include <stdio.h>
#include <math.h>
int main()
{
    // char *str = "qwerty";
    // printf("%s", str);
    // int n = sizeof(str);
    // printf("%d", n);

    // // taking input;
    // char *inp;
    // scanf("%s", inp);

    // The gets() function offers a convenient approach to accepting an entire string from the user, particularly useful for multi-word inputs.
    // A significant advantage of gets() over scanf("%s") is its ability to read an entire line of text
    // without stopping at white-space characters, making it well-suited for inputs containing
    // spaces.
    char *strr = "Hello";
    puts(strr);
    puts("new line");
    return 0;
}